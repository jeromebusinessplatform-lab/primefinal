rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser clients must not write authoritative business data directly.
    // Server-side Admin SDK writes are unaffected by these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
